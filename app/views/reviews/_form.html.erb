<%= form_with(model: [movie,review]) do |form| %>
  <%# if review.errors.any? %>
<!--    <div id="error_explanation">-->
<!--      <h2><%#= pluralize(review.errors.count, "error") %> prohibited this review from being saved:</h2>-->

<!--      <ul>-->
        <%# review.errors.each do |error| %>
<!--          <li><%#= error.full_message %></li>-->
        <%# end %>
<!--      </ul>-->
<!--    </div>-->
  <%# end %>
  <div>
    <%= form.text_area :comment ,placeholder: "Write your Review here..." %>
  </div>
<div>
  <%= form.label 'Rating :' %>
  <%= form.collection_radio_buttons :ratings, (1..5).reverse_each.map { |n| [n, "#{n}⭐"] }, :first, :last do |b| %>
  <%= b.radio_button %>
  <%= b.label(style: 'display: inline') %>
  <% end %>
</div>
  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
